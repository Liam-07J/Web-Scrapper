A simple program that grabs text and tables from a webpage and saves it into a file 

To install all necessary pip packages run isntall.bat

Run main.py to start the program and then enter the url of the webpage you want to scrape
You will also get the option to save the data to a diffrent file with a diffrent name so that when the programs is rerun it will not be overwritten